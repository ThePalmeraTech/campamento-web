<nav class="navbar navbar-expand-lg navbar-white" style="background:rgb(255 255 255 / 53%) !important;">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Campamento Web</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= unauthenticated_root_path %>">Inicio</a>
        </li>
        <% if user_signed_in? && current_user.admin? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= workshops_path %>">Workshops</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= classrooms_path %>">Aulas</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= admin_dashboard_path %>">Admin Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= admin_income_report_path %>">Reporte de Ingresos</a>
          </li>
        <% elsif user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= classrooms_path %>">Mi Aula</a>
          </li>
        <% end %>
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= profile_user_path(current_user) %>">Perfil</a>
          </li>
          <li class="nav-item">
            <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_session_path %>">Iniciar Sesión</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
