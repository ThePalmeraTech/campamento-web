<div class="container mt-5">
  <div class="row">
    <!-- Columna de Login -->
    <div class="col-md-6">
      <h2>Log in</h2>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-horizontal' }) do |f| %>
        <div class="mb-3">
          <%= f.label :email, class: 'form-label' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
        </div>

        <div class="mb-3">
          <%= f.label :password, class: 'form-label' %>
          <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="form-check mb-3">
            <%= f.check_box :remember_me, class: 'form-check-input' %>
            <%= f.label :remember_me, class: 'form-check-label' %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "Log in", class: 'btn btn-primary' %>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>

    <!-- Columna de Registro -->
    <div class="col-md-6">
     <% if available_student_slots > 0 %>
     
     <!-- Dentro de tu vista de Classroom o donde prefieras -->
    <div data-controller="countdown" data-countdown-time-value="<%= @classroom.next_session_start_time %>">
      <h2 id="countdown-display">Tiempo restante: <span data-countdown-target="output"></span></h2>
    </div>


      <h2 class="text-center">Sign up</h2><br /><br />
    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      <center><%= link_to "Iniciar Ahora", new_registration_path(resource_name), class: "btn btn-lg btn-primary" %></center><br />
    <% end %>
    <% else %>
    <h1>Los cupos han sido ocupados</h1>
      <h2>El próximo taller comenzará pronto</h2>
      <p>No pierdas tu cupo para el próximo taller apuntándote a la lista de espera. Recibirás una notificación en tu correo electrónico una vez las suscripciones vuelvan a abrir.</p>
      <a href="#" class="btn btn-info"> Recibir notificación</a>
    <% end %>

    </div>
  </div>
</div>
